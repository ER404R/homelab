apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: traefik
secretGenerator:
  - name: cloudflare-api-token
    envs:
      - .env
    options:
      disableNameSuffixHash: true
